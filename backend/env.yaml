# Cloud Run env vars (used by: gcloud run deploy --env-vars-file=backend/env.yaml)
# Do not put secrets here if this file is committed; use Cloud Run Secrets for those.
CORS_ORIGINS: "https://membercore.io,https://membercore-f0b3f.web.app,https://membercore-f0b3f.firebaseapp.com"
# Firestore project (Cloud Run sets this automatically; override if needed)
GCLOUD_PROJECT: "membercore-f0b3f"
# JWT_SECRET: set in Cloud Run Console → Variables (or --set-env-vars) for production

